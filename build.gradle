allprojects {
    apply plugin: "eclipse"
    apply plugin: "idea"

    version = '1.0'
    ext {
        appName = "MandelbrotSet"
        gdxVersion = '1.10.0'
    }

    repositories {
        mavenCentral()
    }
}
